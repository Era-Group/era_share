<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_cs_followup_compose_form" model="ir.ui.view">
        <field name="name">cs.followup.compose.form</field>
        <field name="model">cs.followup.compose</field>
        <field name="arch" type="xml">
            <form string="Compose Follow-up (AI)">
                <div class="border rounded bg-light p-3 mb-3">
                    <p><strong>What is this screen?</strong> A message-writing assistant for customer follow-up by email, WhatsApp, or SMS.</p>
                    <p><strong>Use it to:</strong></p>
                    <ul>
                        <li>Choose the purpose, channel, and tone of the message.</li>
                        <li>Use recent customer history and an optional service to prepare the draft.</li>
                        <li>Edit the text before opening the selected communication channel.</li>
                    </ul>
                    <p class="mb-0"><strong>Start here:</strong> Select the purpose and tone, generate the draft, then check the recipient, facts, promises, and language. The message is never sent automatically.</p>
                </div>
                <group>
                    <group>
                        <field name="partner_id" readonly="1"/>
                        <field name="channel"/>
                        <field name="tone"/>
                    </group>
                    <group>
                        <field name="purpose" placeholder="e.g. periodic check-in, renewal nudge…"/>
                        <field name="reply_to_last"/>
                        <field name="service_id"/>
                    </group>
                </group>
                <field name="service_image" widget="image" class="oe_avatar"
                       invisible="not service_id" readonly="1"/>
                <group>
                    <button name="action_generate" type="object" string="✦ Generate with AI"
                            class="btn-primary"
                            help="Draft a message using the selected channel, tone, purpose, recent customer timeline, and optional catalog service. Review and edit the draft before opening the channel."/>
                </group>
                <field name="draft" nolabel="1" placeholder="The AI draft appears here — edit freely before sending…"
                       widget="text" options="{'resizable': true}"/>
                <field name="cs_account_id" invisible="1"/>
                <footer>
                    <button name="action_send" type="object" string="Send / Open Channel"
                            class="btn-primary" invisible="not draft"
                            help="Open the selected communication channel with this reviewed draft. Confirm the recipient and content in the channel before final sending."/>
                    <button string="Close" class="btn-secondary" special="cancel"
                            help="Close without opening a channel. The draft is not sent to the customer."/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
