<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <data noupdate="1">

    <!--
      ERA SEO: Weekly audit + AI auto-fix.

      Every Monday morning, runs a fresh audit, then for every
      AI-supported, untouched finding calls action_ai_suggest_and_apply
      (which only applies suggestions whose confidence clears the
      agent's threshold). Users who want to pause it without disabling
      the cron flip `era_seo.weekly_audit_active` to False.
    -->
    <record id="cron_weekly_audit_and_fix" model="ir.cron">
      <field name="name">ERA SEO: Weekly audit + AI auto-fix</field>
      <field name="model_id" ref="model_era_seo_suite_hub"/>
      <field name="state">code</field>
      <field name="code">model.cron_weekly_audit_and_fix()</field>
      <field name="interval_number">1</field>
      <field name="interval_type">weeks</field>
      <field name="active" eval="True"/>
      <field name="priority">6</field>
    </record>

  </data>
</odoo>
